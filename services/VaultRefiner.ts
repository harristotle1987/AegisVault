/**
 * Deprecated due to casing conflict with the project registry. 
 * Unified into services/vaultRefiner.ts (lowercase) to satisfy TS1149 compiler constraints.
 */
export { VaultRefiner } from './vaultRefiner';
